export type SwitchOption = {
  value: any;
  label: string;
};

export type ListItems = {
  label: string;
  value: any;
  row?: string;
};

export type HeaderButton = {
  icon: string;
  href: string;
};
